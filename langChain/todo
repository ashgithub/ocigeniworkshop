Intro
   - types of llms librray
   - SLM - language
   - LLM maturity
   - tour 
- 

----
******* llm- 
chat.py
1. system prompt
2. multiple models
3. batch
4. max token

history.py 

stream.py 
4. streaming 

async.py

structured 
3. structred output




-----

tool calling
   - single step
   - multi step
   - automatic call 
    - react 
   - mcp
  
Agents
   - langchain
   - oci agents (optional)
   - wayflow (optional)
   - langgraph
   - langfuse
   - a2a
  
rag
   - chunking
   - embedding
   - proximity search
   - reranking
   - 
  
******  database: 
    - select ai
    - NL2SQL
    - clsql mcp server 



****** speech
    - tts
      - englihs
      - otehr
      - real  demo in portl 
      - batch
    - stt
      - english
      - other
  
******* vision
    - document understanding
    - vision
    - multi model
  
cline
  - memory bank
  - rules
  - ider
  - workflow

demos
  - ai world demos
  - current projects
  -----
  - Create an isolated checkout and branch:
  - git worktree add ../workshop-langchain-test -b chore/langchain-upgrade-test

- In the new worktree:

  - cd ../workshop-langchain-test
  - uv venv .venv-langchain-test
  - source .venv-langchain-test/bin/activate

- Install current, locked deps:

  - If uv.lock exists: uv sync --frozen
  - If no uv.lock yet: uv lock && uv sync

- Try the new LangChain without touching main:

  - Pin explicitly (examples):

    - uv add "langchain==<target_version>"
    - If needed with split packages: uv add "langchain-core==" "langchain-community==" "langchain-openai=="

  - Or upgrade in-place without changing pyproject pins:
    - uv lock --upgrade-package langchain

- Run your smoke scripts in the sandbox:

  - python langChain/function_calling/langchain_step.py
  - python langChain/llm/openai_oci_chat.py
  - (add any others you rely on)

- Inspect changes:

  - git status; git diff

  - If it works, keep commits on this branch; if not, remove sandbox:

    - deactivate
    - cd ../workshop-langchain-test/.. && git worktree remove workshop-langchain-test
    - git branch -D chore/langchain-upgrade-test
